# interpretator
A custom interpreter for a simple programming language implemented in C++. The project includes lexical analysis, parsing, semantic checking, POLIZ (reverse Polish notation) generation, and program execution. 
